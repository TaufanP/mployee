import {StyleSheet} from 'react-native';
import {SCREEN_HORIZONTAL_PADDING} from '../../../constants/components';

export default StyleSheet.create({
  container: {paddingHorizontal: SCREEN_HORIZONTAL_PADDING, flex: 1},
});
